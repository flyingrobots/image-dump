{
  "name": "image-dump",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "optimize": "node scripts/optimize-images.js",
    "optimize:watch": "node scripts/optimize-images.js --watch",
    "optimize:docker": "docker-compose run --rm optimize",
    "optimize:docker:force": "docker-compose run --rm optimize-force",
    "optimize:docker:lfs": "docker-compose run --rm optimize-lfs",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:docker": "docker-compose run --rm test",
    "test:docker:coverage": "docker-compose run --rm test-coverage",
    "test:docker:watch": "docker-compose run --rm test-watch",
    "docker:build": "docker-compose build",
    "docker:clean": "docker-compose down -v"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "sharp": "^0.34.2",
    "sharp-cli": "^5.1.0"
  }
}
